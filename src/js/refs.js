const menu = document.querySelector('.js-menu');
const body = document.querySelector('body');
const tumbler = document.querySelector(".theme-switch__toggle");

export default {
    menu,
    body,
    tumbler,
}